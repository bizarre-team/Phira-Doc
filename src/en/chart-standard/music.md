# Music File Formats

Currently supported music formats:

- `.mp3`
- `.ogg`
- `.wav`

::: warning Notes
1. `mp3` files may have unpredictable latency. To avoid this issue, use `ogg` whenever possible.
2. `wav` is an uncompressed raw audio format, so it takes up much more space and may cause chart uploads to fail.
:::
