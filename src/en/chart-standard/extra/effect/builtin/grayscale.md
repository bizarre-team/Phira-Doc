# `grayscale`

Grayscale effect.

![Example](image/grayscale.png)

## Parameters

- `factor` (float, default `1.0`): Strength of the grayscale effect. `0` means no grayscale is applied, and `1` means full grayscale.
