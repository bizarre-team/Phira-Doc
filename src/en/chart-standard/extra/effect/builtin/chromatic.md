# `chromatic`

Chromatic aberration effect.

![Example](image/chromatic.png)

## Parameters

- `sampleCount` (int, default `3`, range 1–64): Number of samples. Higher values give smoother results, but cost more performance.
- `power` (float, default `0.01`): Strength of the effect, or in other words, the offset distance.
