# Coordinates

The origin is located at the top-left corner of the first screen after you scroll down one screen on the event page. The X-axis points to the right, and the Y-axis points downward.

The screen width is always `2`. The screen height can be derived from the aspect ratio; see [built-in variables](./variable.md#built-in-variables).
