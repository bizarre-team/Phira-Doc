# Debugging

Build `phira-main` with `--features event_debug`. Then create a file named `test.uml` in the same directory as the Phira executable. After you enter any event, the page content will stay in sync with `test.uml` in real time.
